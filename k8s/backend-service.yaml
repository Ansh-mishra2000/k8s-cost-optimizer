apiVersion: v1
kind: Service

metadata:
  name: k8s-cost-optimizer-service
  namespace: optimizer

spec:

  selector:
    app: k8s-cost-optimizer

  ports:

  - port: 9000
    targetPort: 9000

  type: ClusterIP